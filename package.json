{
  "name": "jquery.magnetize",
  "description": "A plugin in which to have a element in the window (either a navigation block or text/image block) hangs on to be visible as you scroll until the scrolling reachs the end of it's section (it's container).",
  "version": "0.0.1",
  "keywords": [
    "jquery",
    "sticky",
    "magnetize",
    "navigation"
  ],
  "scripts": {
    "start": "babel jquery.magnetize.js -d dist --presets=es2015 --watch",
    "build": "babel --minified jquery.magnetize.js -d dist --presets=es2015"
  },
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "jquery": "^1.11.0"
  },
  "main": "jquery.magnetize.js",
  "repository": "git@github.com:nguyenj/magnetize.git",
  "author": "John Nguyen <hello@jnguyen.me>",
  "license": "MIT"
}
